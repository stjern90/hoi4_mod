POL_plans_for_war = {

	POL_ww2plan = {
	    allowed = {
			always = yes
			#added through on_action
		}
			
		visible = {
			has_defensive_war_with = GER
		}

		available = {
			original_tag = POL
			has_defensive_war_with = GER
		}

		ai_will_do = {
			factor = 200
		}
		
		fire_only_once = yes

		activation = {

		}

		complete_effect = {
			POL = {
				add_timed_idea = {
					idea = POL_determination
					days = 180
				}
			}
		}
	}
}
