	

SWE_unique_measures = {

	SWE_support_the_finnish ={
		icon = generic_prepare_civil_war
		allowed = {
			original_tag = SWE
		}
		available ={
			has_equipment = { 
				infantry_equipment > 999
				support_equipment > 499 
			}

			FIN = {
				has_war_with = SOV
			}
			limit = { 
				NOT = { 
					GER = { 
						has_war_with = SOV 
					}
				}
			}
		}

		visible = {
			FIN = {
				has_war_with = SOV
			}
			has_war = no
		}
		ai_will_do = {
			factor = 10
		}
		days_remove = 15
		days_re_enable = 45
		fire_only_once = no

		complete_effect = {
			ROOT = {
				send_equipment = {
					target = FIN
					type = infantry_equipment
					amount = 1000
					old_prioritised = yes
				}
				send_equipment = {
					target = FIN
					type = support_equipment
					amount = 500
					old_prioritised = yes
				}
			}
		}
	}
	SWE_support_the_anti_commi_war = {

		icon = generic_industry

		allowed = {
			original_tag = SWE
		}

		available = {
				FIN = { 
					has_war_with = SOV
				}
		}

		visible = {
				FIN = { 
					has_war_with = SOV
				}
			num_of_civilian_factories_available_for_projects > 4
		}

		cost = 0
		ai_will_do = {
			factor = 20

			modifier = {
				factor = 0.5
				has_war_support > 0.6
			}
		}

		fire_only_once = no

		modifier = {
			civilian_factory_use = 5			
		}

		days_remove = 180

		complete_effect = {
			random_country = {
				limit = {
					tag = FIN
				}
				add_offsite_building = { type = arms_factory level = 5 }
				set_country_flag = SWE_support_the_anti_commi_war
			}
		}

		remove_effect = {
			random_country = {
				limit = {
					tag = FIN
					has_country_flag = USA_support_the_anti_fascist_war
				}
				
				add_offsite_building = { type = arms_factory level = -5 }
				clr_country_flag = SWE_support_the_anti_commi_war
			}
		}
	}
	SWE_let_volunteers_go = {
		icon = generic_prepare_civil_war
		allowed = {
			original_tag = SWE
		}
		available ={
			FIN = {
				has_war_with = SOV
			}
		}

		visible = {
			FIN = {
				has_war_with = SOV
			}
			has_war = no
		}
		ai_will_do = {
			factor = 10
		}
		cost = 25
		days_re_enable = 60
		fire_only_once = no

		complete_effect = {
			add_manpower = -10000
			random_country = {
				limit = {
					tag = FIN
				}
				add_manpower = 10000
				add_equipment_to_stockpile = { type = artillery_equipment_1 amount = 200 producer = SWE }
				add_equipment_to_stockpile = { type = support_equipment_1 amount = 200 producer = SWE }
				add_equipment_to_stockpile = { type = infantry_equipment amount = 600 producer = SWE }
			}
		}
	}
}