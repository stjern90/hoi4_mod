dont_join_faction_spain = {
	enable = { 
		original_tag = SPR
		has_government = fascism
	}
	
	abort = {
	 
		NOT = {
		has_government = fascism
		}
	}
	

	ai_strategy = {
		type = ignore
		id = GER
		value = 20
	}
    ai_strategy = {
		type = alliance
		id = GER
		value = -130
	}
ai_strategy = {
		type = alliance
		id = ENG
		value = -50
       }
}

dont_join_faction_spain_2 = {
	enable = { 
		original_tag = SPR
		has_government = communism
	}
	
	abort = {
	 
		NOT = {
		has_government = communism
		}
	}
	

	
ai_strategy = {
		type = alliance
		id = ENG
		value = -50
	}
ai_strategy = {
		type = alliance
		id = SOV
		value = -60
	}   
ai_strategy = {
		type = befriend
		id = SOV
		value = 50
	}   
}

# make italy and germany want to send stuff to the fascist side of SCW
ITA_and_GER_help_SPA_win_civil_war = {
	reversed = yes
	enable = {
		date < 1939.1.1
		original_tag = SPR
		has_government = fascism
		SPR = { has_civil_war = yes }
		NOT = { has_global_flag = scw_over }
	}
	abort = {
		OR = {
			date > 1939.1.1
			NOT = { has_government = fascism }
			SPR = { has_civil_war = no }
			has_global_flag = scw_over
		}
	}
	
	ai_strategy = {
		type = send_volunteers_desire
		id = GER
		value = 100
	}

	ai_strategy = {
		type = send_volunteers_desire
		id = ITA
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = GER
		value = 100
	} 
}

# make italy and germany want to send stuff to the fascist side of SCW
SOV_help_SPR_win_civil_war = {
	reversed = yes
	enable = {
		date < 1939.1.1
		original_tag = SPR
		has_government = communism
		SPR = { has_civil_war = yes }
		NOT = { has_global_flag = scw_over }
	}
	abort = {
		OR = {
			date > 1939.1.1
			NOT = { has_government = communism }
			SPR = { has_civil_war = no }
			has_global_flag = scw_over
		}
	}
	
	ai_strategy = {
		type = send_volunteers_desire
		id = SOV
		value = 100
	}
	ai_strategy = {
		type = alliance
		id = SOV
		value = 40
	}  
}
