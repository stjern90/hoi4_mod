add_namespace = law

country_event = { #civ fuel economy law
	id = law.52
	hidden = yes
	
	is_triggered_only = yes
	
	trigger = {
		is_ai = no
		
	}
	
	immediate = {
		if = {
			limit = {
				any_country = {
					has_idea = civ_fuel_consumption_0
				}
			}
			remove_fuel_civ_ideas = yes
		}
		
		if = {
			limit = {
				any_country = {
					has_idea = civ_fuel_consumption_1
				}
			}
			remove_fuel_civ_ideas = yes
			add_fuel_civ_ideas = yes
		}
		
		hidden_effect = { country_event = { id = law.52 days = 2 } }
	
	}
	option = { }
}